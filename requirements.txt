# ReliefMesh backend — requirements.txt
# Frontend deps live in frontend/package.json, this file is Python-only.
#
# Install with:
#   pip install -r requirements.txt --break-system-packages   # if on an externally-managed env
#   pip install -r requirements.txt                            # normal venv

# -- core / already in use ---------------------------------------------------
pydantic>=2.6,<3.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29        # ASGI server; [standard] pulls in websockets + uvloop
python-dotenv>=1.0             # .env config loading

# -- db / audit store ---------------------------------------------------------
sqlalchemy>=2.0,<3.0
asyncpg>=0.29                  # async Postgres driver for SQLAlchemy async engine
psycopg2-binary>=2.9           # sync driver, needed by Alembic migrations
alembic>=1.13                  # DB migrations

# InfluxDB — only needed if you're using it for time-series sensor/report
# ingestion metrics alongside Postgres for the audit store; drop this line
# if you end up going Postgres-only.
influxdb-client>=1.43

# -- optimizer ------------------------------------------------------------
ortools>=9.9                   # CP-SAT solver for constrained mission allocation

# -- api / websocket layer -----------------------------------------------
websockets>=12.0               # WS routes for live map/mission updates
httpx>=0.27                    # async HTTP client, also used by tests for API calls

# -- sim / synthetic data ---------------------------------------------------
faker>=24.0                    # synthetic incident report generation for replay simulator

# -- testing ------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23           # required for the async agent/orchestrator tests